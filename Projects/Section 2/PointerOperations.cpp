// Declare a variable 'myvar' and assign it the value 25
myvar = 25;

// Store the address of 'myvar' in the pointer 'foo'
foo = &myvar;

// Assign the value 20 to 'bar', which will be stored in memory
bar = myvar;

// Dereference 'foo' to get the value it points to (which is 25)
baz = *foo;

// Declare a pointer variable 'name' of type *
type * name;
